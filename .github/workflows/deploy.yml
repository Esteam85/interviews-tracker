name: Build and Deploy
on: [push]
permissions:
  contents: write
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Test 🧪
        run: go test ./...

      - name: Go Get 🧪
        run: go get -u .

      - name: Install and Build 🔧
        run: got build main.go